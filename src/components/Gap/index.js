import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function Gap({ height, widht }) {
    return (
        <View style={{ height: height, width: widht }}>
        </View>
    )
}

const styles = StyleSheet.create({})